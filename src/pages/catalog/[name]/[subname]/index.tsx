import {
    CatalogPage,
    GetCatalogServerSideProps,
    getCatalogServerSideProps
} from "@common/pages/Catalog";

export const getServerSideProps: GetCatalogServerSideProps = getCatalogServerSideProps;

export default CatalogPage;
