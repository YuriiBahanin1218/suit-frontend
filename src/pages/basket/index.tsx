import {BasketPage} from "@common/pages/Basket";
import {
    GetBasketServerSideProps,
    getBasketServerSideProps
} from "@common/pages/Basket/getServerSideProps";

export const getServerSideProps: GetBasketServerSideProps = getBasketServerSideProps;

export default BasketPage;
